import 'package:flutter/material.dart';
class FavoriteScreenBody extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('FavoriteScreenBody'),
      ),
    );
  }
}
